# v0.15.0

Added:

- TS/Next Graph Depth.
- Глубина графа TypeScript/Next.js.
- Route-to-component graph edges for Next.js app routes.
- Client/server boundary metadata from `use client` and app route defaults.
- HTTP method metadata for Next.js `route.ts/js`.
- JS/TS route impact details in impact reports.

Updated:

- Next route nodes connect to local route components and imported JSX components when symbol bindings are available.

Removed:

- Nothing.

Better than v0.14.0:

- Frontend agents get better dependency context for Next.js pages, layouts, and API routes.
- Impact reports carry route-level context without loading full source files.
