# v0.12.0

Added:

- Bidirectional Human Sync.
- Двусторонняя синхронизация Human-слоя.
- `./pmem human sync` pulls manual edits from generated Human notes back into source `knowledge` or `rationale` records.
- Source and body hash metadata in generated Human note frontmatter.
- Conflict detection when both sides changed after the last export.

Updated:

- Human sync regenerates and re-indexes the Human layer after successful updates.

Removed:

- Nothing.

Better than v0.11.0:

- Human notes can now feed corrected context back into durable project memory.
- Sync conflicts are explicit and safe.
