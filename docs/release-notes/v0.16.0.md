# v0.16.0

Added:

- Memory Quality Dashboard.
- Панель качества памяти.
- `./pmem report` for local Markdown reports.
- `./pmem report --format json`.

Updated:

- Reports include index freshness, graph counts, conflicts, active tasks, eval hints, and module state.

Removed:

- Nothing.

Better than v0.15.0:

- Agents can inspect memory health through one bounded local command.
- The report summarizes local state without loading large files into context.
