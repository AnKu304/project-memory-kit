# v0.17.0

Added:

- Human Graph Viewer.
- Визуальный просмотр графа Human-слоя.
- `./pmem human graph --html`.
- Static local `graph.html` with layer/type/status filters and search.

Updated:

- Human graph JSON includes status for filtering.

Removed:

- Nothing.

Better than v0.16.0:

- Human memory can be inspected visually without a local server or extra dependencies.
- The graph view makes it easier to find related knowledge and rationale before opening full notes.
