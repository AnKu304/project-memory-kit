interface:
  display_name: "Dependency Graph RAG"
  short_description: "Mandatory local project-memory workflow for impact-aware coding."
  default_prompt: "Use $dependency-graph-rag to build change context, check impact, run targeted tests, and update failure memory."

policy:
  allow_implicit_invocation: true

metadata:
  installer: project-memory-kit
  command_unix: ./pmem
  command_windows: .\pmem.ps1
