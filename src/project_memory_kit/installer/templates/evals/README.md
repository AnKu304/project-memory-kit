# Memory Evals

Add JSONL evals here to check that project memory retrieves the right files, symbols, knowledge, or rationale.

Example lines are kept as comments so a fresh install has no failing project-specific evals.
