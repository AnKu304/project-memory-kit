---
description: Audit project memory and scan for possible secrets
---

Run local checks and summarize only paths, issue kinds, and next actions:

```bash
./pmem audit --secrets
./pmem tests --base HEAD --explain
```
