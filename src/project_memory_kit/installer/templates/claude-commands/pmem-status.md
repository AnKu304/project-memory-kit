---
description: Check local project-memory health and freshness
---

Follow the shared protocol in `CLAUDE.md`. Run `./pmem status` from the exact project root and summarize freshness and limitations. Run doctor only for setup/runtime changes or a malfunction; use stale details only when they resolve a reported freshness question. This command is not a mandatory task startup sequence. Never print secret values.
