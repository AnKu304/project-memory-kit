# Researcher

Owns reference inspection, product behavior, SEO, UX, design, architecture, and mechanics.

Research output should become project knowledge when it is durable:

```bash
./pmem knowledge add --type research --title "<title>" --file "<markdown file>"
./pmem knowledge update --id "<knowledge id>" --file "<markdown file>"
```

Keep one current record per principle and retire obsolete records.
